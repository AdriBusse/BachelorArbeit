export const sendRequestWithSW = () => {
    describe("Send a Request while SW ist registered", function () {
        it("should send a request while SW is registered", function () {
            // 1: Register service worker.
            // 2: Wait for service worker to install.
            // 3: Check cache was performed correctly.
        });
    });
}